{
  "name": "everbee-store-app-mcp",
  "version": "1.0.0",
  "description": "MCP server for an EverBee Store app — generates tools from the app's live OpenAPI spec",
  "type": "module",
  "bin": { "everbee-store-app-mcp": "dist/index.js" },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.2"
  }
}
